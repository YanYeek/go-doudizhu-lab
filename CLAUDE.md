# CLAUDE.md

## 仓库定位

用 Go 实现斗地主（Dou Dizhu）核心玩法的实验项目。重点是游戏逻辑本身：发牌、叫地主、牌型判定（单张/对子/三带/顺子/飞机/炸弹/王炸等）、出牌合法性校验、对局状态机。当前为刚初始化状态，代码从零开始。

## 目录约定

计划采用 Go 社区惯例布局（随开发逐步建立）：

```
cmd/doudizhu/    # 程序入口（main 包）
internal/
  card/          # 牌的定义、牌组、洗牌发牌
  pattern/       # 牌型识别与比较
  game/          # 对局流程、状态机、规则
```

- 业务逻辑放 `internal/`，不对外暴露
- 测试文件与被测代码同目录（`xxx_test.go`），牌型判定这类纯逻辑必须有表驱动测试

## 常用命令

```bash
go run ./cmd/doudizhu   # 运行
go test ./...           # 全量测试
go vet ./...            # 静态检查
gofmt -l .              # 检查格式
```

## 注意事项

- 模块路径在 `go mod init` 时确定，新建包时 import 路径以 go.mod 为准
- 提交信息用一句话中文，不带 emoji
